{
  "name": "music-kb",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "description": "Personal music-tutorial knowledge base. TanStack Start + Strapi, with AI-generated summaries and an interactive theory companion.",
  "type": "module",
  "scripts": {
    "client": "yarn --cwd ./client dev",
    "test": "yarn --cwd ./packages/music test && yarn --cwd ./client test && yarn --cwd ./web test",
    "server": "yarn --cwd ./server develop",
    "setup:client": "node ./copy-env.mjs ./client",
    "setup:server": "node ./copy-env.mjs ./server",
    "setup": "yarn install && yarn setup:client && yarn setup:server",
    "dev": "yarn concurrently \"cd server && yarn develop\" \"npx wait-on http://localhost:1350 && cd client && yarn dev\"",
    "start": "./start.sh",
    "start:fresh": "./start.sh --restart-ollama",
    "export": "yarn --cwd ./server export",
    "seed": "yarn --cwd ./server seed",
    "db:dump:local": "yarn --cwd ./server db:dump:local",
    "db:dump:neon": "yarn --cwd ./server db:dump:neon",
    "db:load:local": "yarn --cwd ./server db:load:local",
    "db:load:neon": "yarn --cwd ./server db:load:neon",
    "web": "yarn --cwd ./web dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.0.0",
    "wait-on": "^8.0.1"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "client",
    "server",
    "web",
    "packages/*"
  ]
}
