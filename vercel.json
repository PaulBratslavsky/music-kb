{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "yarn --cwd packages/music install --frozen-lockfile && yarn --cwd web install --frozen-lockfile",
  "buildCommand": "yarn --cwd web build",
  "outputDirectory": "web/dist"
}
