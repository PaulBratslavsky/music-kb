{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "yarn --cwd packages/music install && yarn --cwd web install",
  "buildCommand": "yarn --cwd web build",
  "outputDirectory": "web/dist"
}
